# macOS
.DS_Store
**/.DS_Store

# editor swap files
.*.swp
*.swp

# session state (claude /remember)
.remember/

# host development linked worktrees
/.worktrees/

# OMP runtime lock (config.yml symlink target)
/omp/config.yml.lock
